% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_gsf_recovery.R
\docType{data}
\name{gsf_recovery}
\alias{gsf_recovery}
\title{Simulated Data for Parameter Recovery Checks}
\format{
A data frame with 50 observations and the following columns:
\describe{
\item{firm_id}{Integer panel identifier (10 firms).}
\item{time_id}{Integer period indicator (5 periods per firm).}
\item{Y}{Output in levels.}
\item{L, K}{Input levels (labor and capital).}
\item{CR, MKSH, FP, IMP}{Exogenous determinants of input slacks.}
}
}
\source{
Generated via \code{simulate_gsf_data(seed = 2025)}; see
\code{inst/scripts/simulate_data.R} for implementation details.
}
\usage{
gsf_recovery
}
\description{
\code{gsf_recovery} is a compact panel generated from the same data-generating
process used in the Stan model. It is meant for unit tests or vignettes that
demonstrate how the package can recover production-function parameters,
slack determinants, and inefficiency profiles when the model is correctly
specified.
}
\details{
The object carries two attributes:
\itemize{
\item \code{attr(., "true_params")} — list containing the production-function
coefficients, covariance parameters, and slack determinants used to
generate the data.
\item \code{attr(., "true_efficiency")} — list containing the exact input
slacks and output technical inefficiency for every observation,
expressed in percentage terms.
}

Analysts can fit the model via:
\preformatted{
  fit <- gsf_fit(
    log(Y) ~ log(L) + log(K),
    data = gsf_recovery,
    z_vars = c("CR", "MKSH", "FP", "IMP")
  )
}
and then compare \code{efficiency_summary(fit)} or \code{coef(fit)} against the stored
ground truth.
}
\keyword{datasets}
