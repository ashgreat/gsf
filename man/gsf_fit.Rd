% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsf_fit.R
\name{gsf_fit}
\alias{gsf_fit}
\title{Fit Generalized Stochastic Frontier Model}
\usage{
gsf_fit(
  formula,
  data,
  z_vars = NULL,
  neutral_vars = NULL,
  panel_id = NULL,
  time_id = NULL,
  chains = 4,
  iter = 2000,
  warmup = 1000,
  thin = 1,
  cores = parallel::detectCores() - 1,
  prior_sigma_scale = 1,
  prior_sigma_u_scale = 0.5,
  prior_delta_scale = 10,
  prior_omega_scale = 0.1,
  seed = NULL,
  ...
)
}
\arguments{
\item{formula}{A formula specifying the production function. The left-hand
side should be the log output, and the right-hand side should contain
the log inputs (e.g., log(Y) ~ log(L) + log(K)).}

\item{data}{A data frame containing the variables.}

\item{z_vars}{Character vector of variable names that determine input
slacks. These are the exogenous variables that affect input effectiveness.
If NULL, only stochastic slacks are estimated.}

\item{neutral_vars}{Character vector of variable names that act as neutral
technology shifters (affect output directly). If NULL, no neutral shifters.}

\item{panel_id}{Character string naming the panel identifier variable.
If NULL, cross-sectional data is assumed.}

\item{time_id}{Character string naming the time identifier variable.
Required if panel_id is specified.}

\item{chains}{Number of MCMC chains (default: 4).}

\item{iter}{Number of iterations per chain (default: 2000).}

\item{warmup}{Number of warmup iterations (default: 1000).}

\item{thin}{Thinning rate (default: 1).}

\item{cores}{Number of cores for parallel computation (default: parallel::detectCores() - 1).}

\item{prior_sigma_scale}{Prior scale for noise standard deviation (default: 1).}

\item{prior_sigma_u_scale}{Prior scale for output inefficiency (default: 0.5).}

\item{prior_delta_scale}{Prior scale for regression coefficients (default: 10).}

\item{prior_omega_scale}{Prior scale for slack covariance (default: 0.1).}

\item{seed}{Random seed for reproducibility.}

\item{...}{Additional arguments passed to rstan::sampling.}
}
\value{
An object of class "gsf_fit" containing:
\item{stanfit}{The rstan fit object}
\item{data}{The processed data}
\item{model_info}{Information about the model specification}
\item{call}{The original function call}
}
\description{
Fits the Generalized Stochastic Frontier (GSF) model from Kumbhakar and
Tsionas (2021) that jointly estimates output technical inefficiency and
input-specific inefficiencies (slacks).
}
\examples{
\dontrun{
# Simulate data
set.seed(123)
n <- 100
data <- data.frame(
  Y = exp(rnorm(n, 5, 0.5)),
  L = exp(rnorm(n, 3, 0.3)),
  K = exp(rnorm(n, 4, 0.4)),
  competition = runif(n, 0, 1)
)

# Fit model
fit <- gsf_fit(
  formula = log(Y) ~ log(L) + log(K),
  data = data,
  z_vars = "competition",
  chains = 2,
  iter = 1000
)

# Summary
summary(fit)
}

}
